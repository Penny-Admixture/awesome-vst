# PostgreSQL connection — matches the Docker container created by install.ps1
DATABASE_URL=postgresql://roseglassdb:roseglassdb@localhost:5432/roseglassdb

# Where ffmpeg-sliced audio files are written
SAMPLES_DIR=samples
